[build]
  builder = "nixpacks"
  buildCommand = "pip install -r requirements.txt"

[service]
  startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  restart = "always"

[volumes]
  "uploads" = { mount = "app/uploads" }
